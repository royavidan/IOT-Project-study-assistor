# Feature: simulator

Dev-only data simulator (`/simulator`; CLI in `scripts/simulator.ts`).
- `simulator-env.ts` — synthetic env/session generation. `simulator.functions.ts` — server fn.

Shared used: `@/lib/focus-load`, `@/lib/queries/sessions`.
