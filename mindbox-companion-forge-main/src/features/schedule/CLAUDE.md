# Feature: schedule

Calendar / focus-block scheduling (`/calendar`).
- `schedule.ts` — schedule domain logic. `queries.ts` — schedule queries.
- `components/{ScheduleEventDialog,ScheduleTimeline,CourseBuilderDialog}`.

Shared used: `@/lib/queries/sessions`, `@/lib/session-scope`, `@/lib/supabase`.
